[project]
name = "aip-cli"
version = "0.1.0"
requires-python = ">=3.11"
description = "A command-line interface for AI Provider tools"
dependencies = [
    "typer>=0.26.8",
]

[project.scripts]
aip = "aip.cli:app"

[tool.uv.sources]